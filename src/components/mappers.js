//array of items that will be rendered at the same time in the format of INputs components which takes in props object
var mappers = ["First item", "second Item", "Third item"];

export default mappers;
